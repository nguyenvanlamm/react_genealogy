import { createSlice } from '@reduxjs/toolkit';
const initialState = {
 
};
const newsSlice = createSlice({
  name: 'news',
  initialState,
  reducers: {
   
  }
});
export default newsSlice.reducer;
